修前 = 7c4754a^ 的 skills/build-batch/batch.py -> C:\Users\user\AppData\Local\Temp\prevdiff127-mfpy769y\batch_before.py
修後 = 工作區 D:\Self Project\Skills\skills\build-batch\batch.py
母體 24 批(有重複票號 11、沒有 13)

批次                          該不該變      修前          修後          判定
b01_normal_two              不該變       exit 0      exit 0      OK              [逐字相同]
b02_normal_five             不該變       exit 0      exit 0      OK              [逐字相同]
b03_empty                   不該變       exit 0      exit 0      OK              [逐字相同]
b04_single_fast             不該變       exit 0      exit 0      OK              [逐字相同]
b05_single_slow             不該變       exit 0      exit 0      OK              [逐字相同]
b06_no_titles_key           不該變       exit 0      exit 0      OK              [逐字相同]
b07_titles_partial          不該變       exit 0      exit 0      OK              [逐字相同]
b08_titles_empty            不該變       exit 0      exit 0      OK              [逐字相同]
b09_typo_only               不該變       exit 1      exit 1      OK              [逐字相同]
b10_judgement_only          不該變       exit 1      exit 1      OK              [逐字相同]
b11_typo_and_judgement      不該變       exit 1      exit 1      OK              [逐字相同]
b12_legit_override          不該變       exit 0      exit 0      OK              [逐字相同]
b13_wide_numbers            不該變       exit 0      exit 0      OK              [逐字相同]
a01_dupe_pair               該變        exit 0      exit 1      OK              [有差額]
a02_dupe_triple             該變        exit 0      exit 1      OK              [有差額]
a03_dupe_identical          該變        exit 0      exit 1      OK              [有差額]
a04_dupe_plus_typo          該變        exit 1      exit 1      OK              [有差額]
a05_dupe_plus_judgement     該變        exit 1      exit 1      OK              [有差額]
a06_dupe_and_clean_mix      該變        exit 0      exit 1      OK              [有差額]
a07_two_different_dupes     該變        exit 0      exit 1      OK              [有差額]
a08_dupe_non_adjacent       該變        exit 0      exit 1      OK              [有差額]
a09_dupe_no_title           該變        exit 0      exit 1      OK              [有差額]
a10_dupe_plus_separate_typo 該變        exit 1      exit 1      OK              [有差額]
a11_dupe_str_vs_int         該變        exit 0      exit 0      探針              [逐字相同]

==== 差額 10 筆(逐筆原文)====

  [a01_dupe_pair] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(2 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #47 登入頁 — 覆蓋 1 條驗收項
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #47  分級:慢 — 覆蓋 1 條驗收項
    --- 修後 exit=1
    | 分級(1 張、2 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a02_dupe_triple] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(3 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #47 登入頁 — 覆蓋 1 條驗收項
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #47  分級:慢 — 覆蓋 1 條驗收項
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    --- 修後 exit=1
    | 分級(1 張、3 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 3 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢(改不了)  #47 登入頁 — 這個票號在這批出現 3 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 3 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47(重複 3 次) 的分級改不了,理由跟著各自那一列

  [a03_dupe_identical] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(2 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    --- 修後 exit=1
    | 分級(1 張、2 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a04_dupe_plus_typo] 預期內(#127 要修的那一面)
    --- 修前 exit=1
    | 分級(2 張,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #9 — 你填的分級只能是「快」或「慢」,你打的是 'fast' —— 改一下再重跑
    |   慢          #9 — 覆蓋 1 條驗收項
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #9
    !err 停在這裡 —— #9 的分級改不了,理由跟著各自那一列
    --- 修後 exit=1
    | 分級(1 張、2 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #9 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑;另外,你填的分級只能是「快」或「慢」,你打的是 'fast' —— 改一下再重跑
    |   慢(改不了)  #9 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #9
    !err 停在這裡 —— #9(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a05_dupe_plus_judgement] 預期內(#127 要修的那一面)
    --- 修前 exit=1
    | 分級(2 張,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   慢(改不了)  #47 登入頁 — 動到判斷邏輯或資料寫入,硬規則一律慢 —— 改不成快。驗收清單第 4 條就是它。要改快只有一條路:回去改票的內容,把動到判斷邏輯或資料寫入的那部分切出去,再重切一次分級。票的內容沒變就是慢,你說了也一樣
    |   慢          #47 登入頁 — 覆蓋 1 條驗收項
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47 的分級改不了,理由跟著各自那一列
    --- 修後 exit=1
    | 分級(1 張、2 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   慢(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑;另外,動到判斷邏輯或資料寫入,硬規則一律慢 —— 改不成快。驗收清單第 4 條就是它。要改快只有一條路:回去改票的內容,把動到判斷邏輯或資料寫入的那部分切出去,再重切一次分級。票的內容沒變就是慢,你說了也一樣
    |   慢(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a06_dupe_and_clean_mix] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(4 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #48 結帳流程 — 覆蓋 1 條驗收項
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   快  #49 寄信 — 沒有覆蓋驗收項,不會有你看得到的行為
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #48  分級:慢 — 覆蓋 1 條驗收項
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #49  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    --- 修後 exit=1
    | 分級(3 張、4 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢          #48 結帳流程 — 覆蓋 1 條驗收項
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   快          #49 寄信 — 沒有覆蓋驗收項,不會有你看得到的行為
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a07_two_different_dupes] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(4 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   快  #48 結帳流程 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #47 登入頁 — 覆蓋 1 條驗收項
    |   慢  #48 結帳流程 — 覆蓋 1 條驗收項
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #48  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #47  分級:慢 — 覆蓋 1 條驗收項
    |   #48  分級:慢 — 覆蓋 1 條驗收項
    --- 修後 exit=1
    | 分級(2 張、4 列,其中 2 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   快(改不了)  #48 結帳流程 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢(改不了)  #48 結帳流程 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    |   #48 結帳流程
    !err 停在這裡 —— #47(重複 2 次)、#48(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a08_dupe_non_adjacent] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(4 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #48 結帳流程 — 覆蓋 1 條驗收項
    |   慢  #49 寄信 — 覆蓋 1 條驗收項
    |   慢  #47 登入頁 — 覆蓋 1 條驗收項
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #48  分級:慢 — 覆蓋 1 條驗收項
    |   #49  分級:慢 — 覆蓋 1 條驗收項
    |   #47  分級:慢 — 覆蓋 1 條驗收項
    --- 修後 exit=1
    | 分級(3 張、4 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢          #48 結帳流程 — 覆蓋 1 條驗收項
    |   慢          #49 寄信 — 覆蓋 1 條驗收項
    |   慢(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    !err 停在這裡 —— #47(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a09_dupe_no_title] 預期內(#127 要修的那一面)
    --- 修前 exit=0
    | 分級(2 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #88 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #88 — 覆蓋 1 條驗收項
    | 
    | 點頭之後,每張票上會多這一行:
    |   #88  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #88  分級:慢 — 覆蓋 1 條驗收項
    --- 修後 exit=1
    | 分級(1 張、2 列,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #88 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   慢(改不了)  #88 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #88
    !err 停在這裡 —— #88(重複 2 次) 的分級改不了,理由跟著各自那一列

  [a10_dupe_plus_separate_typo] 預期內(#127 要修的那一面)
    --- 修前 exit=1
    | 分級(3 張,其中 1 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快          #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   快          #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   快(改不了)  #48 結帳流程 — 你填的分級只能是「快」或「慢」,你打的是 'fast' —— 改一下再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #48 結帳流程
    !err 停在這裡 —— #48 的分級改不了,理由跟著各自那一列
    --- 修後 exit=1
    | 分級(2 張、3 列,其中 2 張改不了)— 標「慢」的會演給你看,標「快」的不會:
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   快(改不了)  #47 登入頁 — 這個票號在這批出現 2 次 —— 同一張票只能有一列,刪掉多的再重跑
    |   快(改不了)  #48 結帳流程 — 你填的分級只能是「快」或「慢」,你打的是 'fast' —— 改一下再重跑
    | 
    | 這批還不能貼 —— 下面這幾張的分級改不了,先解決再整批重跑一次:
    |   #47 登入頁
    |   #48 結帳流程
    !err 停在這裡 —— #47(重複 2 次)、#48 的分級改不了,理由跟著各自那一列

  修前 exit 0(#127 講的安靜失敗,這次修掉的就是這些):['a01_dupe_pair', 'a02_dupe_triple', 'a03_dupe_identical', 'a06_dupe_and_clean_mix', 'a07_two_different_dupes', 'a08_dupe_non_adjacent', 'a09_dupe_no_title']
  修前就非 0(同批另有打錯字 / 硬規則,#118 那條路本來就擋):['a04_dupe_plus_typo', 'a05_dupe_plus_judgement', 'a10_dupe_plus_separate_typo']

==== 探針的現況(不計入判定)====
  a11_dupe_str_vs_int: 修前修後逐字相同 —— #127 沒碰到這個形狀
    --- 現況(修後) exit=0
    | 分級(2 張)— 標「慢」的會演給你看,標「快」的不會:
    |   快  #47 登入頁 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   慢  #47 — 覆蓋 1 條驗收項
    | 
    | 點頭之後,每張票上會多這一行:
    |   #47  分級:快 — 沒有覆蓋驗收項,不會有你看得到的行為
    |   #47  分級:慢 — 覆蓋 1 條驗收項

母體 24,不合 0
