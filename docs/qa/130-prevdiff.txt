# /qa #130 修前對照 —— 同一份母體,修前(2ebbc89^)vs 修後(工作區 = 2ebbc89)
#
# #130 動的是判斷邏輯(入口的型別收斂 + 印出來的指令形狀),所以「修後 self-check
# 綠」證明不了「本來好的批次沒被改壞」。這支把修前那份 batch.py 從 git 取出來,
# 對 130-wide.py 的**同一份母體**(180 批)跑一遍,判準也用 130-wide.py 的 check()
# —— 這條 lane 自己的第二把尺,不是受測物那面。
#
# 三欄:修前紅/修後綠(這張票修掉的)、修前綠/修後紅(本輪引入 -> blocking)、
# 兩邊都紅。另外單獨列「行為有差」讓收緊面看得見。

==== $ python scripts/qa/130-prevdiff.py
修前 = 2ebbc89^ 的 batch.py,修後 = 工作區
母體 180 批(= 130-wide.py 的 cases())

== 修前紅 / 修後綠 —— 這張票修掉的:156 批 ==
  interrupted / 純 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 純 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 純 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 純 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 純 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 純 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 純 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 純 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 純 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 純 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 純 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 純 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 純 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 純 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 純 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 純 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 純 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec=None
        修前 [W1] 照抄貼的指令 `/build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec=True
        修前 [W1] 照抄貼的指令 `/build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec=[]
        修前 [W1] 照抄貼的指令 `/build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec=''
        修前 [W1] 照抄貼的指令 `/build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 純 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 純 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 純 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 純 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 字串混 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 字串混 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 字串混 int / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 字串混 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 字串混 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 字串混 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 字串混 int / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 字串混 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 字串混 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 字串混 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 字串混 int / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 字串混 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 字串混 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 字串混 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 字串混 int / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 字串混 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec=None
        修前 [W1] 照抄貼的指令 `/build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec=True
        修前 [W1] 照抄貼的指令 `/build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec=[]
        修前 [W1] 照抄貼的指令 `/build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec=''
        修前 [W1] 照抄貼的指令 `/build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 字串混 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 字串混 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 字串混 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 字串混 int / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 帶空白 / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 帶空白 / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 帶空白 / spec=' 108 '
        修前 [W1] 照抄貼的指令 `/client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108 ` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 帶空白 / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 帶空白 / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 帶空白 / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 帶空白 / spec='\xa0108'
        修前 [W1] 照抄貼的指令 `/client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo # 108` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 帶空白 / spec='１０８'
        修前 [W1] 照抄貼的指令 `/build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 帶空白 / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 帶空白 / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 帶空白 / spec='１０８'
        修前 [W1] 照抄貼的指令 `/client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #１０８` 裡 `#` 後面是 '１０８' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 帶空白 / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 全綠 / 帶空白 / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  merged 有人還在修 / 帶空白 / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  summary / 帶空白 / spec='0108'
        修前 [W4] spec 算出來是 #108,但 stdout 上找不到 `#108`
  interrupted / 帶空白 / spec=108.0
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec=108.0
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec='108.0'
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec='108.0'
        修前 [W1] 照抄貼的指令 `/client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #108.0` 裡 `#` 後面是 '108.0' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec=None
        修前 [W1] 照抄貼的指令 `/build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec=None
        修前 [W1] 照抄貼的指令 `/client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #None` 裡 `#` 後面是 'None' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec=True
        修前 [W1] 照抄貼的指令 `/build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec=True
        修前 [W1] 照抄貼的指令 `/client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #True` 裡 `#` 後面是 'True' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec=[]
        修前 [W1] 照抄貼的指令 `/build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec=[]
        修前 [W1] 照抄貼的指令 `/client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #[]` 裡 `#` 後面是 '[]' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec='一〇八'
        修前 [W1] 照抄貼的指令 `/client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #一〇八` 裡 `#` 後面是 '一〇八' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec=''
        修前 [W1] 照抄貼的指令 `/build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec=''
        修前 [W1] 照抄貼的指令 `/client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #` 裡 `#` 後面是 '' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec='#108'
        修前 [W1] 照抄貼的指令 `/build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec='#108'
        修前 [W1] 照抄貼的指令 `/client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo ##108` 裡 `#` 後面是 '#108' —— 不是純數字,貼進終端機不會跑到那張票
  interrupted / 帶空白 / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `/build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$build-batch #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 全綠 / 帶空白 / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  merged 有人還在修 / 帶空白 / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
  summary / 帶空白 / spec='https://example.invalid/issues/108'
        修前 [W1] 照抄貼的指令 `/client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票
        修前 [W1] 照抄貼的指令 `$client-demo #https://example.invalid/issues/108` 裡 `#` 後面是 'https://example.invalid/issues/108' —— 不是純數字,貼進終端機不會跑到那張票

== 修前綠 / 修後紅 —— 本輪引入的 regression:0 批 ==
  (無)

== 兩邊都紅:0 批 ==
  (無)

== 兩邊都綠:24 批 ==

== 行為有差(exit code 或 stdout 不逐字相同):156 批 ==
  interrupted / 純 int / spec=' 108 '
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch # 108 `(Codex: `$build-batch # 108 `)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec=' 108 '
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec='\xa0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #\xa0108`(Codex: `$build-batch #\xa0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec='\xa0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec='１０８'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #１０８`(Codex: `$build-batch #１０８`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec='１０８'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec='0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #0108`(Codex: `$build-batch #0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec='0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec=108.0
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=108.0
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='108.0'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='108.0'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=None
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #None`(Codex: `$build-batch #None`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=None
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=True
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #True`(Codex: `$build-batch #True`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=True
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=[]
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #[]`(Codex: `$build-batch #[]`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=[]
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='一〇八'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #一〇八`(Codex: `$build-batch #一〇八`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='一〇八'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=''
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #`(Codex: `$build-batch #`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=''
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #`(Codex: `$client-demo #`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='#108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch ##108`(Codex: `$build-batch ##108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='#108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #https://example.invalid/issues/108`(Codex: `$build-batch #https://example.invalid/issues/108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=' 108 '
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch # 108 `(Codex: `$build-batch # 108 `)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec=' 108 '
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #\xa0108`(Codex: `$build-batch #\xa0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec='１０８'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #１０８`(Codex: `$build-batch #１０８`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec='１０８'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec='0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #0108`(Codex: `$build-batch #0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec='0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec=108.0
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=108.0
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='108.0'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='108.0'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=None
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #None`(Codex: `$build-batch #None`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=None
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=True
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #True`(Codex: `$build-batch #True`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=True
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=[]
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #[]`(Codex: `$build-batch #[]`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=[]
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='一〇八'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #一〇八`(Codex: `$build-batch #一〇八`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='一〇八'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=''
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #`(Codex: `$build-batch #`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=''
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #`(Codex: `$client-demo #`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='#108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch ##108`(Codex: `$build-batch ##108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='#108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #https://example.invalid/issues/108`(Codex: `$build-batch #https://example.invalid/issues/108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=' 108 '
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch # 108 `(Codex: `$build-batch # 108 `)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec=' 108 '
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #\xa0108`(Codex: `$build-batch #\xa0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec='１０８'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #１０８`(Codex: `$build-batch #１０８`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec='１０８'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec='0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #0108`(Codex: `$build-batch #0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec='0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec=108.0
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=108.0
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='108.0'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='108.0'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=None
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #None`(Codex: `$build-batch #None`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=None
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=True
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #True`(Codex: `$build-batch #True`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=True
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=[]
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #[]`(Codex: `$build-batch #[]`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=[]
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='一〇八'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #一〇八`(Codex: `$build-batch #一〇八`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='一〇八'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=''
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #`(Codex: `$build-batch #`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=''
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #`(Codex: `$client-demo #`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='#108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch ##108`(Codex: `$build-batch ##108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='#108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #https://example.invalid/issues/108`(Codex: `$build-batch #https://example.invalid/issues/108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`)'
        修後 exit=1 末行=''
exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch # 108 `(Codex: `$build-batch # 108 `)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec=' 108 '
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec='\xa0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #\xa0108`(Codex: `$build-batch #\xa0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec='\xa0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec='１０８'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #１０８`(Codex: `$build-batch #１０８`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec='１０８'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec='0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #0108`(Codex: `$build-batch #0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 純 int / spec='0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 純 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 純 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 純 int / spec=108.0
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=108.0
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='108.0'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='108.0'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=None
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #None`(Codex: `$build-batch #None`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=None
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=True
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #True`(Codex: `$build-batch #True`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=True
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=[]
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #[]`(Codex: `$build-batch #[]`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=[]
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='一〇八'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #一〇八`(Codex: `$build-batch #一〇八`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='一〇八'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec=''
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #`(Codex: `$build-batch #`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec=''
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #`(Codex: `$client-demo #`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='#108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch ##108`(Codex: `$build-batch ##108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='#108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`)'
        修後 exit=1 末行=''
  interrupted / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #https://example.invalid/issues/108`(Codex: `$build-batch #https://example.invalid/issues/108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 純 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=' 108 '
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch # 108 `(Codex: `$build-batch # 108 `)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec=' 108 '
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #\xa0108`(Codex: `$build-batch #\xa0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec='１０８'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #１０８`(Codex: `$build-batch #１０８`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec='１０８'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec='0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #0108`(Codex: `$build-batch #0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 字串混 int / spec='0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 字串混 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 字串混 int / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 字串混 int / spec=108.0
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=108.0
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='108.0'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='108.0'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=None
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #None`(Codex: `$build-batch #None`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=None
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=True
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #True`(Codex: `$build-batch #True`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=True
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=[]
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #[]`(Codex: `$build-batch #[]`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=[]
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='一〇八'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #一〇八`(Codex: `$build-batch #一〇八`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='一〇八'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec=''
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #`(Codex: `$build-batch #`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec=''
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #`(Codex: `$client-demo #`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='#108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch ##108`(Codex: `$build-batch ##108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='#108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`)'
        修後 exit=1 末行=''
  interrupted / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #https://example.invalid/issues/108`(Codex: `$build-batch #https://example.invalid/issues/108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 字串混 int / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=' 108 '
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch # 108 `(Codex: `$build-batch # 108 `)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec=' 108 '
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec=' 108 '
        修前 exit=0 末行='下一步:`/client-demo # 108 `(Codex: `$client-demo # 108 `)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #\xa0108`(Codex: `$build-batch #\xa0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec='\xa0108'
        修前 exit=0 末行='下一步:`/client-demo #\xa0108`(Codex: `$client-demo #\xa0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec='１０８'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #１０８`(Codex: `$build-batch #１０８`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec='１０８'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec='１０８'
        修前 exit=0 末行='下一步:`/client-demo #１０８`(Codex: `$client-demo #１０８`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec='0108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #0108`(Codex: `$build-batch #0108`)會接續這條 lane'
        修後 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108`(Codex: `$build-batch #108`)會接續這條 lane'
  merged 全綠 / 帶空白 / spec='0108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 一次 demo 這批'
        修後 exit=0 末行='2 張已合併,下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 一次 demo 這批'
  merged 有人還在修 / 帶空白 / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`) — 先 demo 已收的 1 張'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`) — 先 demo 已收的 1 張'
  summary / 帶空白 / spec='0108'
        修前 exit=0 末行='下一步:`/client-demo #0108`(Codex: `$client-demo #0108`)'
        修後 exit=0 末行='下一步:`/client-demo #108`(Codex: `$client-demo #108`)'
  interrupted / 帶空白 / spec=108.0
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=108.0
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=108.0
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='108.0'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #108.0`(Codex: `$build-batch #108.0`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='108.0'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='108.0'
        修前 exit=0 末行='下一步:`/client-demo #108.0`(Codex: `$client-demo #108.0`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=None
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #None`(Codex: `$build-batch #None`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=None
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=None
        修前 exit=0 末行='下一步:`/client-demo #None`(Codex: `$client-demo #None`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=True
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #True`(Codex: `$build-batch #True`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=True
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=True
        修前 exit=0 末行='下一步:`/client-demo #True`(Codex: `$client-demo #True`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=[]
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #[]`(Codex: `$build-batch #[]`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=[]
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=[]
        修前 exit=0 末行='下一步:`/client-demo #[]`(Codex: `$client-demo #[]`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='一〇八'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #一〇八`(Codex: `$build-batch #一〇八`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='一〇八'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='一〇八'
        修前 exit=0 末行='下一步:`/client-demo #一〇八`(Codex: `$client-demo #一〇八`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec=''
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #`(Codex: `$build-batch #`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec=''
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #`(Codex: `$client-demo #`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec=''
        修前 exit=0 末行='下一步:`/client-demo #`(Codex: `$client-demo #`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='#108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch ##108`(Codex: `$build-batch ##108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='#108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='#108'
        修前 exit=0 末行='下一步:`/client-demo ##108`(Codex: `$client-demo ##108`)'
        修後 exit=1 末行=''
  interrupted / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='中斷,可續 #48 結帳 — 未合併,工作區 .git/batch-worktrees/48 與 branch batch/48 都留著;重跑 `/build-batch #https://example.invalid/issues/108`(Codex: `$build-batch #https://example.invalid/issues/108`)會接續這條 lane'
        修後 exit=1 末行=''
  merged 全綠 / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='2 張已合併,下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 一次 demo 這批'
        修後 exit=1 末行=''
  merged 有人還在修 / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`) — 先 demo 已收的 1 張'
        修後 exit=1 末行=''
  summary / 帶空白 / spec='https://example.invalid/issues/108'
        修前 exit=0 末行='下一步:`/client-demo #https://example.invalid/issues/108`(Codex: `$client-demo #https://example.invalid/issues/108`)'
        修後 exit=1 末行=''
exit=0

==== 判讀 ====
- 修前紅 / 修後綠 **156 批** —— 就是 180 減掉本來就對的 24 批(spec 給 `108` 與
  `"108"` 這兩種寫法 x 4 mode x 3 shape)。剩下 13 種寫法在修前全部有問題:
  * `" 108 "` / `"\u00a0108"`:印成 `/build-batch # 108`,`#` 後面帶空白,bash 與
    PowerShell 都當註解起頭,client 照抄貼上去等於什麼都沒跑(W1 + W4)。
  * 全形 `"１０８"`:印成 `#１０８`,終端機不認(W1 + W4)。
  * `"0108"`:印成 `#0108`,不是 GitHub 上那張 #108(W4)。
  * `108.0` / `"108.0"`:印成 `#108.0`,指到一張不存在的票(W1)。
  * `null` / `true` / `[]` / `"一〇八"` / `""` / `"#108"` / URL:修前一路印出去,
    印出來的是 `#None` / `#True` / `#[]` / `#一〇八` / `#` / `##108` / `#https://…`
    —— 全部 exit 0、stderr 空的,沒有任何聲音(W1)。修後這幾種當場停,訊息指名
    `spec` 那格、講得出他填了什麼,stdout 是空的(W3 / W5 全綠)。

- 修前綠 / 修後紅 **0 批** —— 本輪沒有引入新的誤判。

- 兩邊都紅 **0 批**。

- 兩邊都綠 **24 批** —— `spec` 本來就寫成 `108` 或 `"108"` 的那些,修前修後逐字
  相同(「行為有差」那一欄 156 批,正好等於修前紅那 156 批,沒有多出來的)。

==== 判定 ====
差額全部落在該修的那一面,沒有本輪引入的 regression。這條對照:綠。
